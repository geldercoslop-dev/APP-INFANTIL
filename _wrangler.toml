# Cloudflare Pages Build Configuration

[build]
  command = "npm run build:prod"
  publish = "dist"

[[redirects]]
  pattern = "/*"
  zone = "example.com"
  destination = "/index.html"
  status = 200

[env.production]
  NODE_VERSION = "18"
